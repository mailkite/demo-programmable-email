services:
  - type: web
    name: demo-programmable-email
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.mjs
    envVars:
      - key: MAILKITE_API_KEY
        sync: false
      - key: MAILKITE_WEBHOOK_SECRET
        sync: false
